import { BlogBodyDirective } from './blog-body.directive';

describe('BlogBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new BlogBodyDirective();
    expect(directive).toBeTruthy();
  });
});
